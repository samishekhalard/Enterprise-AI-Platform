name: API Contract Security

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  openapi-governance:
    name: OpenAPI Governance
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Install ripgrep
        run: sudo apt-get install -y ripgrep

      - name: Transport-security governance
        run: ./scripts/check-transport-security-baseline.sh

      - name: Spectral lint
        run: |
          npx --yes @stoplight/spectral-cli@6.13.1 lint \
            --ruleset .spectral.yaml \
            backend/*/openapi.yaml

      - name: Fetch base branch
        run: git fetch origin main --depth=1

      - name: OpenAPI breaking-change check
        run: ./scripts/check-openapi-breaking.sh origin/main

  gateway-security-contracts:
    name: Gateway Security Contracts
    runs-on: ubuntu-latest
    env:
      COMPOSE_PROJECT_NAME: emsist-ci-security
      CONTRACT_BASE_URL: http://localhost:28080
      CONTRACT_TENANT_ID: 00000000-0000-0000-0000-000000000001
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: "npm"
          cache-dependency-path: contracts/package-lock.json

      - name: Install contract test dependencies
        working-directory: contracts
        run: npm ci

      - name: Start data tier (valkey)
        run: docker compose -f docker-compose.dev-data.yml up -d valkey

      - name: Ensure all networks exist for app tier
        run: |
          docker network create ems-dev-backend 2>/dev/null || true
          docker network create ems-dev-frontend 2>/dev/null || true
          docker network ls --filter name=ems-dev

      - name: Wait for valkey health
        run: |
          for i in {1..30}; do
            if docker compose -f docker-compose.dev-data.yml exec -T valkey valkey-cli ping 2>/dev/null | grep -q PONG; then
              echo "Valkey is healthy"
              exit 0
            fi
            sleep 2
          done
          echo "Valkey did not become healthy in time"
          exit 1

      - name: Build and start gateway
        run: docker compose -f docker-compose.dev-app.yml up -d --build --no-deps api-gateway

      - name: Wait for API Gateway health
        run: |
          for i in {1..60}; do
            if curl -fsS http://localhost:28080/actuator/health > /dev/null; then
              echo "Gateway is healthy"
              exit 0
            fi
            sleep 2
          done
          echo "Gateway did not become healthy in time"
          docker compose -f docker-compose.dev-app.yml logs api-gateway
          exit 1

      - name: Run gateway security contract tests
        working-directory: contracts
        run: npm test

      - name: Shutdown gateway stack
        if: always()
        run: |
          docker compose -f docker-compose.dev-app.yml down -v || true
          docker compose -f docker-compose.dev-data.yml down -v || true
